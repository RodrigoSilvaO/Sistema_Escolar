<!-- consultar.jinja2 -->

{% extends "_base.jinja2" %}

{% block nav_title %}SISTEMA ESCOLAR{% endblock %}
{% block title %}{{ titulo }}{% endblock %}

{% block content %}

<h2 class="text-center" style="margin-bottom: 20px;">
    {{ titulo }}
</h2>

<!-- Renderize a tabela usando o macro (veja arquivo templates/_macros.jinja2) -->
{{ macros.render_table(cabecalho, dados) }}

{% if api_atualizar %}
<form class="text-center" action="{{ api_atualizar }}" method="get">
    <!-- campos_chave: lista de (label, nome_do_campo) que identificam o registro -->
    {% for label, nome in campos_chave %}
    {{ macros.render_input(label, "text", nome, "") }}
    {% endfor %}

    <button type="submit" class="btn btn-primary">Atualizar</button>
</form>
{% endif %}

<form class="text-center" style="margin-top: 15px;" action="{{ api_apagar }}" method="post">
    <!-- campos_chave: lista de (label, nome_do_campo) que identificam o registro -->
    {% for label, nome in campos_chave %}
    {{ macros.render_input(label, "text", nome, "") }}
    {% endfor %}

    <button type="submit" class="btn btn-danger">Apagar</button>
</form>

{% endblock %}
